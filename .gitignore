node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime database: contains real volunteer/family PII and (hashed) credentials
# once the app is used for real. Never commit it.
/data/
